<% if current_customer.present? && current_customer != customer %>
  <% if current_customer.following?(customer) %>
    <%= link_to "フォロー中", public_customer_relationships_path(customer.id), method: :delete, class: "btn_g_s", remote: true %>
  <% else %>
    <%= link_to "フォロー", public_customer_relationships_path(customer.id), method: :post, class: "btn_y_s", remote: true %>
  <% end %>
<% end %>