<h3>ジャンル編集</h3>

<%= form_with model: @genre, url: admin_genre_path(@genre.id), method: :patch do |f| %>
  <%= f.label :"ジャンル名" %>
  <%= f.text_field :name, include_blank: "ジャンル名は必須です。", required: true %>
  <%= f.submit "変更を保存" %>
<% end %>